# Hard requirements — the pipeline cannot run without these.
networkx>=3.0
requests>=2.28

# Soft/optional — the pipeline degrades gracefully without these:
#   matplotlib   only used if you manually plot the raw graph yourself
#   anthropic    only needed if ANTHROPIC_API_KEY is set (Claude provider)
#   supabase     only needed if SUPABASE_URL/SUPABASE_KEY are set (DB sync)
# Groq and Gemini (the two free LLM providers) only need `requests` above —
# you do NOT need to install anthropic or supabase to run this end-to-end.
matplotlib>=3.7
anthropic>=0.40.0
supabase>=2.4.0
